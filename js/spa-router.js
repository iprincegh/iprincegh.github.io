/**
 * SPA Router — disabled to fix navigation issues.
 * Using standard multi-page navigation instead.
 * The globe video gracefully restarts on each page load.
 */
(function () {
  'use strict';
  // No SPA routing — let browser handle navigation normally.
  // This prevents broken animations, missing scripts, and page loading issues.
})();
